if [[ "$1" == "-h" || "$1" == "--help" ]]; then
  echo -e "\nAjoute un fichier, commit et push."
  echo "Usage: gacp fichier message"
  echo "Exemple: gacp toto.txt 'ajout toto'"
  exit 0
fi
git add "$1"
git commit -m "$2"
git push
